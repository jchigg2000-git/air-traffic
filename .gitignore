.DS_Store

# Go build artifacts
/air-traffic-server
/air-traffic-gateway
*.test

# Durable local state: harness (ratchet series, promoted corpus, pattern pack)
# and the keystore (keys.json — digests, but still not ours to commit)
/data/

# Phase 2 frontend
web/node_modules/
web/dist/

# Local secrets minted by scripts/dev-env.sh
.env

# Claude Code ratchet-up scratch (per-run patches + ledger)
.claude/ratchet-up/
